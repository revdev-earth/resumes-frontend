export const WaitingRoom = () => {
  // here we are/
  // from where we come?

  // is it public?

  // is it private?

  // the person come from resumes.rev.earth/name
  // this is public

  // the person come from login
  // it is private
  // so we know him id
  // first bring the business card
  // then templates
  // in te

  // has a resume
  // count resumes
  // then decide
  // how many resumes can a persona have?
  // just 3

  return <></>
}
